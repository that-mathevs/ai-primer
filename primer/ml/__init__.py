"""
# Part 1: how the model works inside

From a single neuron to a working transformer, and how models are trained and
served. Embeddings, the centerpiece, have their own package:
`primer.ml.embeddings`.
"""

from primer.curriculum import reading_list as _reading_list

__doc__ += _reading_list("primer.ml")
